#include <stdio.h>

int main() {
    
    int n1, 2, soma;
    printf("Digite o primeiro número: ");
    scanf("%d", 1);
    printf("Digite o segundo número: ");
    scanf("%d", 2);

    soma = n1 + 2;

    printf("A soma é: %d\n", soma);
    return 0;
    #include <stdio.h>
}